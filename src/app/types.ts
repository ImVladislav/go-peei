export type navLinks = {
  href: string;
  key: string;
  label: string;
};

export type projectItem = {
  id: string;
  title: string;
  imageSrc: string;
  description: string;
};
export type reviewItem = {
  id: string;
  name: string;
  description: string;
  imageSrc: string;
};
